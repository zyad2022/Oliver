{
  "entities": {
    "User": {
      "title": "User Profile",
      "description": "Stores user profile data and basic account info.",
      "type": "object",
      "properties": {
        "uid": { "type": "string", "description": "Firebase Auth UID" },
        "name": { "type": "string", "description": "Display name of the user" },
        "email": { "type": "string", "description": "Email address of the user" },
        "createdAt": { "type": "timestamp" }
      },
      "required": ["uid", "name", "email", "createdAt"]
    },
    "Order": {
      "title": "Order",
      "description": "Stores user order history.",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "items": { "type": "array" },
        "totalItems": { "type": "number" },
        "subtotal": { "type": "number" },
        "totalPrice": { "type": "number" },
        "paymentMethod": { "type": "string" },
        "status": { "type": "string" },
        "createdAt": { "type": "timestamp" }
      },
      "required": ["userId", "items", "totalItems", "subtotal", "totalPrice", "paymentMethod", "status", "createdAt"]
    },
    "Review": {
      "title": "Customer Review",
      "description": "Stores customer feedback and ratings.",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "comment": { "type": "string" },
        "rating": { "type": "number" },
        "createdAt": { "type": "timestamp" }
      },
      "required": ["name", "comment", "rating", "createdAt"]
    },
    "ContactMessage": {
      "title": "Contact Message",
      "description": "Stores messages sent via the contact form.",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "email": { "type": "string" },
        "message": { "type": "string" },
        "createdAt": { "type": "timestamp" }
      },
      "required": ["name", "email", "message", "createdAt"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": { "$ref": "#/entities/User" },
      "description": "User profiles collection, keyed by user UID."
    },
    "/users/{userId}/orders/{orderId}": {
      "schema": { "$ref": "#/entities/Order" },
      "description": "User specific orders collection."
    },
    "/reviews/{reviewId}": {
      "schema": { "$ref": "#/entities/Review" },
      "description": "Customer reviews collection."
    },
    "/contact_messages/{messageId}": {
      "schema": { "$ref": "#/entities/ContactMessage" },
      "description": "Messages from the contact form."
    }
  }
}
